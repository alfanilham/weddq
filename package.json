{
  "name": "weddq",
  "version": "1.0.0",
  "private": true,
  "description": "weddQ — Indonesian digital wedding invitation platform",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm-run-all -p dev:*",
    "dev:api": "npm --prefix apps/api run dev",
    "dev:web": "npm --prefix apps/web run dev",
    "build": "npm --prefix apps/api run build && npm --prefix apps/web run build",
    "db:up": "docker compose up -d postgres adminer",
    "db:down": "docker compose down",
    "db:migrate": "npm --prefix apps/api run db:migrate",
    "db:seed": "npm --prefix apps/api run db:seed",
    "db:reset": "npm --prefix apps/api run db:reset",
    "setup": "npm install && npm run db:up && sleep 4 && npm run db:migrate && npm run db:seed"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
